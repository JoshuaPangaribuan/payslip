# Server Config
server.address.http=:8081

# Salt Bcrypt
bcrypt.cost=10

# JWT Secret
jwt.secret=verysecret

# Database Config
database.host=db
database.port=5432
database.name=payslip
database.user=root
database.password=rootpassword
database.query.dialect=postgres